en:
  log:
    export:
      start: "## start %{type} export ##"
      end: "## end %{type} export ##"
      start_for_store: "start export in store %{store_name} (%{store_id})"
      feed_already_launch: "feed already launch"
      nb_product_found: "%{nb_product} product(s) found"
      count_product: "%{product_count} products exported"
      total_product_exported: "%{nb_product} product(s) exported (%{nb_simple} simple product(s), %{nb_configurable} configurable product(s), %{nb_grouped} grouped product(s), %{nb_virtual} virtual product(s), %{nb_downloadable} downloadable product(s))"
      memory_usage: "memory usage: %{memory} Mb"
      execution_time: "execution time: %{seconds} seconds"
      generate_feed_available_here : "export of the store %{store_name} (%{store_id}) generated a file here : %{feed_url}"
      unauthorised_ip: "unauthorised IP : %{ip}"
      error_configurable_product_disabled: "WARNING ! %{nb_product} simple product(s) are associated with configurable products disabled"
      error_folder_not_created: "can't create folder %{folder_path}"
      error_folder_not_writable: "can't access folder %{folder_path}"
    import:
      not_valid_action: "Action: %{action} is not a valid action"
      preprod_mode_active: "warning ! Preprod mode is activated"
      start: "## start %{type} import ##"
      end: "## end %{type} import ##"
      start_for_store: "start import in store %{store_name} (%{store_id})"
      connector_get_order: "get order with order id: %{marketplace_sku} and marketplace: %{markeplace_name}"
      connector_get_all_order: "get orders between %{date_from} and %{date_to} with account ID: %{account_id}"
      find_one_order: "%{nb_order} order found for order ID: %{marketplace_sku} and markeplace: %{markeplace_name} with account ID: %{account_id}"
      find_all_orders: "%{nb_order} order(s) found with account ID: %{account_id}"
      external_id_exist: "already imported in a Magento with order ID %{order_id}"
      order_shipped_by_marketplace: "order shipped by %{markeplace_name}"
      current_order_state_unavailable: "current order's state [%{order_state_marketplace}] makes it unavailable to import for marketplace %{marketplace_name}"
      import_failed: "import failed - %{decoded_message}"
      lengow_order_saved: "order saved in lengow orders table"
      lengow_order_not_saved: "WARNING ! Order could NOT be saved in lengow orders table"
      lengow_order_updated: "order updated in lengow orders table"
      lengow_order_line_saved: "save order lines product : %{order_line_saved}"
      rewrite_processing_fee: "rewrite amount without processing fee"
      rewrite_shipping_cost: "rewrite amount without shipping cost"
      shipping_method_unavailable: "the selected shipping method is unavailable for current order. Lengow shipping method assigned"
      generate_unique_email: "generate unique email %{email}"
      product_be_found: "product id %{product_id} found with field %{attribute_name} (%{attribute_value})"
      product_state_canceled: "product %{product_id} could not be added to cart - status: %{state_product}"
      order_already_imported: "order already imported (ORDER ID %{order_id})"
      order_ready_to_reimport: "order is ready to be re-imported (ORDER ID %{order_id})"
      order_state_updated: "order's state has been updated to %{state_name}"
      order_successfully_imported: "order successfully imported (ORDER ID %{order_id})"
      order_import_failed: "import order failed - %{decoded_message}"
      error_value_required: "%{key} is required to create Lengow order"
      error_already_created: "%{decoded_message} (created on the %{date_message})"
      error_no_package: "import order failed - no package in the order"
      error_wrong_package_number: "import order failed - wrong package number"
      error_no_delivery_address: "import order failed - no delivery address in the order"
      order_synchronized_with_lengow: "order successfully synchronised with Lengow webservice (ORDER ID %{order_id})"
      order_not_synchronized_with_lengow: "WARNING ! Order could NOT be synchronised with Lengow webservice (ORDER ID %{order_id})"
      quantity_back_reimported_order: "adding quantity back to stock (order is re-imported)"
      quantity_back_shipped_by_marketplace : "adding quantity back to stock (order shipped by marketplace)"
    mail_report:
      send_mail_to: "report email sent to %{email}"
      unable_send_mail_to: "unable to send report email to %{email}"
    order_action:
      start_for_store: "check finish actions in store %{store_name} (%{store_id})"
      call_tracking: "call tracking with parameters: %{parameters}"
      call_action_failed: "order action failed - %{decoded_message}"
    connector:
      call_api: "call %{curl_url}"
      error_api: "call api failed - %{error_code}"
    setting:
      setting_change: "%{key} - old value %{old_value} change by %{value}"
      setting_change_for_store: "%{key} - old value %{old_value} change by %{value} for store %{store_id}"