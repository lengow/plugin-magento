en:
  log:
    export:
      start: "## start export ##"
      end: "## end export ##"
      start_for_shop: "start export in shop %{name_shop} (%{id_shop})"
      feed_already_launch: "feed already launch"
      nb_product_found: "%{nb_product} product(s) found"
      count_product: "%{product_count} products exported"
      total_product_exported: "%{nb_product} product(s) exported (%{nb_simple} simple product(s), %{nb_configurable} configurable product(s), %{nb_bundle} bundle product(s), %{nb_grouped} grouped product(s), %{nb_virtual} virtual product(s))"
      memory_usage: "memory usage: %{memory} Mb"
      execution_time: "execution time: %{seconds} seconds"
      your_feed_available_here: "your feed is available here: <a href='%{feed_url}' target='_blank'>%{feed_url}</a>"
      generate_feed_available_here : "export of the store %{name_shop} (%{id_shop}) generated a file here : %{feed_url}"
      unauthorised_ip: "unauthorised IP : %{ip}"
      error_configurable_product_disabled: "WARNING ! %{nb_product} simple product(s) are associated with configurable products disabled"
      error_folder_not_created: "can't create folder %{folder_path}"
      error_folder_not_writable: "can't access folder %{folder_path}"
    import:
      not_valid_action: "Action: %{action} is not a valid action"
      preprod_mode_active: "warning ! Preprod mode is activated"
      start: "## start %{type} import ##"
      end: "## end %{type} import ##"
      start_for_shop: "start import in shop %{name_shop} (%{id_shop})"
      connector_get_order: "get order with order id: %{marketplace_sku} and marketplace: %{markeplace_name}"
      connector_get_all_order: "get orders between %{date_from} and %{date_to} with account ID: %{account_id}"
      find_one_order: "%{nb_order} order found for order ID: %{marketplace_sku} and markeplace: %{markeplace_name} with account ID: %{account_id}"
      find_all_orders: "%{nb_order} order(s) found with account ID: %{account_id}"
      error_already_created: "%{decoded_message} (created on the %{date_message})"
      external_id_exist: "already imported in a Magento with order ID %{order_id}"
      import_failed: "import failed - %{decoded_message}"
      order_already_imported: "order already imported (ORDER ID %{order_id})"
      order_ready_to_reimport: "order is ready to be re-imported (ORDER ID %{order_id})"
      order_state_updated: "order's state has been updated to %{state_name}"
      order_import_failed: "import order failed - %{decoded_message}"
      error_no_package: "import order failed - no package in the order"
      error_wrong_package_number: "import order failed - wrong package number"
      error_no_delivery_address: "import order failed - no delivery address in the order"
    connector:
      call_api: "call %{curl_url}"
      error_api: "call api failed - %{error_code}"
    setting:
      setting_change: "%{key} - old value %{old_value} change by %{value}"
      setting_change_for_shop: "%{key} - old value %{old_value} change by %{value} for shop %{shop_id}"